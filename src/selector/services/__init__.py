"""services — application services that orchestrate engines.

The instance/fleet manager (route to the focused game) and the MCP diagnostics
server (read-only introspection over localhost for Claude Code).
"""
